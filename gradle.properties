# Desactivamos el cache de configuración que puede ser inestable en CI
org.gradle.configuration-cache=false

# Optimizamos memoria para el runner de GitHub Actions (7GB total)
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=false
